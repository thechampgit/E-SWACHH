# Build Project Workflow

This workflow details the commands required to install dependencies and build the e-Swachh Next.js application.

## Steps

1. **Install Dependencies**:
   ```bash
   npm install
   ```

2. **Run TypeScript Typecheck**:
   ```bash
   npm run typecheck
   ```

3. **Build the Application**:
   ```bash
   npm run build
   ```
